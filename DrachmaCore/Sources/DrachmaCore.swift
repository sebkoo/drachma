/// DrachmaCore is the shared model layer — the "M" in MVVM — packaged so every
/// surface (iOS app, widgets, the drachma-mcp server) consumes the same models,
/// networking, and caching.
public enum DrachmaCore {
    public static let version = "0.0.1"
}
