import DrachmaCore

// Placeholder entry point — the MCP Swift SDK wiring (tools: convert,
// latest_rates, historical) lands in an upcoming commit. Depending on
// DrachmaCore from day one proves the shared-core layout compiles.
print("drachma-mcp \(DrachmaCore.version)")
